name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build web app
        run: npm run build
      
      - name: Generate app icons
        run: |
          npx @capacitor/assets generate --platform android --source public/icons/icon-512.png 2>/dev/null || true
      
      - name: Remove broken android and create fresh
        run: |
          rm -rf android
          npx cap add android
          npx cap sync android
      
      - name: Copy generated icons to android
        run: |
          if [ -d "resources/android" ]; then
            cp -r resources/android/* android/app/src/main/res/ 2>/dev/null || true
          fi
      
      - name: Build APK
        run: cd android && ./gradlew assembleDebug
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: matric-mind-ai.apk
          path: android/app/build/outputs/apk/debug/app-debug.apk